
const SectionTitle = ({ heading }) => {
    return (
        <div>
            <h1 className="text-4xl font-bold text-center pt-20">{heading}</h1>
        </div>
    );
};

export default SectionTitle;